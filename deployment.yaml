apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend-deployment
  namespace: backend
  labels:
    app: backend-springboot
spec:
  replicas: 2
  selector:
    matchLabels:
      app: backend-springboot
  template:
    metadata:
      labels:
        app: backend-springboot
    spec:
      containers:
        - name: backend-container
          image: 908027375897.dkr.ecr.ap-south-1.amazonaws.com/java-backend:latest
          ports:
            - containerPort: 8080
          env:
            - name: BACKEND_USERNAME
              valueFrom:
                secretKeyRef:
                  name: java-db-secret
                  key: BACKEND_USERNAME
            - name: BACKEND_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: java-db-secret
                  key: BACKEND_PASSWORD
            - name: BACKEND_URL
              valueFrom:
                secretKeyRef:
                  name: java-db-secret
                  key: BACKEND_URL
            - name: SERVER_PORT
              valueFrom:
                secretKeyRef:
                  name: java-db-secret
                  key: SERVER_PORT
